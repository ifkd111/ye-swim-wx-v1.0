# 版本记录

## v1.0.0

- 新建“叶”微信小程序独立项目。
- 登录页只保留账号密码入口，按 `admin` / `jl*` / `xy*` 自动进入角色工作台。
- 品牌统一为单字“叶”，登录页使用单字叶形 logo。
- 支持管理员、教练、学员三角色。
- 支持账号密码首次登录绑定微信。
- 支持学员预约、管理员审批生成排课、教练确认出勤。
- 支持微信云开发数据库和 mock 模式。
- 提供 seed 导出、项目校验和规则测试脚本。
